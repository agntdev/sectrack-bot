[
  {
    "name": "Start Learning",
    "steps": [
      {
        "send": { "callback": "learn:select_topic" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": { "text": "Pick a topic to start learning:" }
          }
        ]
      }
    ]
  },
  {
    "name": "Start Learning shows topic options",
    "steps": [
      {
        "send": { "callback": "learn:select_topic" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "editMessageText" }
        ]
      }
    ]
  }
]
